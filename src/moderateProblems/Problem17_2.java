package moderateProblems;
/*
 * Design an algorithm to figure out if someone has won a game of tic-tac-tow
 */
/*
public class Problem17_2 {

	public static void main (String args[]){

	}

	int N=4;// board.lengh;
	int row=0;
	int col=0;
	 
	for (row = 0; row < N; row++) {
		if (board[row][0] != Piece.Empty) {
			for (col = 1; col < N; col++) {
				if (board[row][col] != board[row][col-l]) break;
			}
			if (col == N) return board[row][0];
		}
	}
	 
	for (col = 0; col < N; col++) {
		if (board[0][col] != Piece.Empty) {
			for (row = 1; row < N; row++) {
				if (board[row][col] != board[row-l][col]) break;
			}
		}



	}
*/